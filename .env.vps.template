# MA ERP-CRM — VPS Environment Variables
# Copy this to /root/home/deploy/apps/ma-erp/.env and fill in all values
# The API server loads this file automatically on startup.

# ── Database ──────────────────────────────────────────────────────
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/ma_erp

# ── Auth ──────────────────────────────────────────────────────────
SESSION_SECRET=CHANGE_ME_LONG_RANDOM_STRING_AT_LEAST_32_CHARS

# ── Server ────────────────────────────────────────────────────────
PORT=8080
NODE_ENV=production

# ── Auto-deploy webhook ───────────────────────────────────────────
DEPLOY_SECRET=0d1a95ed3a4a67ec0282d2bc9685bee4d2880c09662a2470e4b269751c485106

# ── WhatsApp (optional) ───────────────────────────────────────────
# WHATSAPP_ACCESS_TOKEN=
# WHATSAPP_VERIFY_TOKEN=
# WHATSAPP_APP_SECRET=
# WHATSAPP_PHONE_NUMBER_ID=

# ── SMTP Email (optional) ─────────────────────────────────────────
# SMTP_HOST=
# SMTP_USER=
# SMTP_PASS=

# ── Expo Push Notifications (optional) ───────────────────────────
# EXPO_ACCESS_TOKEN=
